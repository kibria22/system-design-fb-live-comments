apiVersion: v1
kind: Service
metadata:
  name: comments-service
  namespace: fb-live-comments
  labels:
    app: comments-service
spec:
  type: ClusterIP
  selector:
    app: comments-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
