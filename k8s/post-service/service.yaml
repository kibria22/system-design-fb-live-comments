apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: fb-live-comments
  labels:
    app: post-service
spec:
  type: ClusterIP
  selector:
    app: post-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
